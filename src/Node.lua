Node = class('Node')

function Node:initialize(x, y)
  self.x = x
  self.y = y
  
  if(self.x - 
  
  self.neighborNodes = {upLeft = node:initialize
  
  self.upLeft = 
  self.upRight = 
  self.downLeft = 
  self.downRight =
  
end
